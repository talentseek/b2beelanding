import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "B2Bee - AI Automation for Small Business | 24/7 Digital Workers",
  description: "AI Bees that work 24/7 bringing you the honey. Automate social media, sales outreach, and more. No days off, no sick leave, no breaks.",
  keywords: ["AI automation", "small business", "social media automation", "sales automation", "B2Bee"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
